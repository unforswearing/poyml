#!/bin/bash

# using a yaml file to store default pandoc arguments
# see: https://pandoc.org/MANUAL.html#defaults-files

# pandoc --defaults=settings.yaml -i input.md -o output.file
# pandoc --defaults=collection.yaml
